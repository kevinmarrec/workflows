{
  "type": "module",
  "private": true,
  "packageManager": "bun@1.3.11",
  "license": "MIT",
  "workspaces": [
    "filesize-diff",
    "gitops-render"
  ],
  "scripts": {
    "check": "bun run lint && bun run knip && bun run typecheck",
    "check:artifacts": "bun run build:actions && ! (git status --porcelain | grep '^.M' | grep dist)",
    "build:actions": "bun -F '*' build",
    "knip": "knip -n",
    "lint": "eslint .",
    "prepare": "bunx simple-git-hooks",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "@kevinmarrec/eslint-config": "^1.22.0",
    "@kevinmarrec/tsconfig": "^1.22.0",
    "@types/node": "^26.4.0",
    "@vitest/coverage-v8": "4.1.11",
    "eslint": "^10.9.1",
    "knip": "^6.33.0",
    "typescript": "~6.0.3",
    "vitest": "^5.0.0"
  },
  "simple-git-hooks": {
    "pre-commit": "echo 'Pre-commit checks' && bun run check:artifacts"
  }
}
