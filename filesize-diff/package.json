{
  "name": "filesize-diff-action",
  "type": "module",
  "private": true,
  "description": "GitHub Action for analyzing file size differences between a branch and the main branch",
  "main": "dist/run.js",
  "scripts": {
    "build": "bun build src/run.ts --outdir dist --target node --minify --sourcemap"
  },
  "dependencies": {
    "@actions/cache": "^5.0.0",
    "@actions/core": "^2.0.0",
    "@actions/github": "^6.0.1",
    "filesize": "^11.0.13",
    "pathe": "^2.0.3",
    "tinyglobby": "^0.2.15"
  }
}
